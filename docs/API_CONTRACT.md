# Contrat d'API — source de vérité des endpoints

**Statut : VALIDÉ le 17/09/2026** (réponses aux questions ouvertes et aux 15 questions de comportement appliquées, voir `DECISIONS.md` D-16 à D-35), **complété le 19/09/2026** par D-40 à D-43 (avoir, absence, libellés d'examen, devise). Plus rien de suspendu.
Toute route créée, renommée ou modifiée doit l'être ici dans le même commit (règle d'or n° 1 de `CLAUDE.md`).

## Méthode de construction

Point de départ : les **écrans** de `mobile-app/src/screens/` réellement atteignables depuis `AppNavigator.tsx`, puis les méthodes de `mobile-app/src/services/api/*Service.ts` qu'ils appellent, puis les chemins dans `src/config/api.config.ts`. Les routes définies dans `api.config.ts` mais qu'aucun écran n'appelle sont listées en §9 et **seront supprimées du mobile** (D-13), pas implémentées.

Côté backend : `services/*/src/routes/*.routes.ts`, controllers et validators Joi.

### Légende des statuts

| Statut | Signification |
|---|---|
| **EXISTE** | La route existe côté backend avec la même méthode, le même chemin et un payload/réponse compatibles avec la **cible** décrite. Un bug d'exécution peut subsister (colonne « Écart / notes ») mais le contrat est le bon. |
| **MANQUE** | Aucune route backend ne répond à ce chemin. |
| **DIVERGE** | Une route backend existe au même chemin (ou au plus proche) mais son rôle, son payload, sa réponse ou sa sémantique diffèrent de la cible. |
| **SUPPRIMÉE** | Route abandonnée par décision ; à retirer du mobile, ne pas implémenter. |

Chaque ligne décrit la **cible** (ce que le mobile doit envoyer / recevoir une fois la Phase 5–6 livrée) et, dans « Écart / notes », l'état actuel du backend et du mobile.

### Conventions transverses (valables pour toutes les lignes)

- Préfixe `/api/…`, joint via Nginx sur `:80`.
- Auth : header `Authorization: Bearer <accessToken>` sauf mention « public ». `accessToken` 1 h, `refreshToken` 30 j avec rotation (D-12, D-23).
- Succès : objet ou tableau **nu** en JSON, dates ISO 8601, clés camelCase.
- Erreur : **`{ error: <code stable>, message: <texte français> }`** (D-27). Codes utilisés dans ce document : `VALIDATION_ERROR` (400), `UNAUTHORIZED` (401), `FORBIDDEN` / `FORBIDDEN_SCHOOL` (403), `NOT_FOUND` (404), `CONFLICT` (409), `CANCEL_WINDOW_CLOSED` (403), `NOT_ENROLLED` (403), `PRICE_REQUIRED` (400), `INVALID_SCHOOL_CODE` (400). Le mobile **traduit le code** dans la langue de l'utilisateur (français ou arabe, D-47) et n'affiche `message` que si le code est inconnu, puis un texte propre à l'écran (`getApiErrorMessage`, `src/services/api/ApiError.ts`, 6.1 puis 10.5). Les messages du backend restent français (D-27) : ajouter un code au contrat implique donc d'ajouter sa traduction dans `mobile-app/src/i18n/{fr,ar}.ts`.
- Identifiants : **`:studentId` et `studentId` dans tout payload/réponse = `users.id`** (D-28). Le backend résout `students.id` en interne. `instructorId` = `instructors.id`. `schoolId` = `schools.id`.
- Vocabulaire (D-18) : leçons `CODE` / `Manœuvre` / `Parc` ; examens `theory` / `practical` ; résultats `passed` / `failed` / `pending`. Le mobile utilise les mêmes valeurs (enums `LessonType`, `ExamType`, `ExamResult` de `src/models/`, libellés d'affichage à côté — 6.1).
- Cloisonnement (D-20) : toute action d'un `instructor` est limitée à son école → 403 `FORBIDDEN_SCHOOL` sinon. Non répété ligne par ligne.
- Une seule inscription active par élève (D-22) : les routes qui prennent `:schoolId` sont conservées ; celles qui n'en prennent pas résolvent l'école depuis l'inscription active.
- Identité de l'appelant : `req.user = { userId, email, role }` posé par le middleware unique de `services/api` (D-03). Le bug historique `req.user.userId === undefined` des anciens services a disparu avec eux (2.7).
- Devise (D-43) : chaque école a une devise ISO 4217 (`School.currency`, `TND` par défaut) ; tous les montants d'une école (`Pricing.price`, `price` / `amount` des leçons et examens, `FinancialSummary`) sont des nombres nus dans cette devise. Le mobile la lit par S2 (cache par école) et l'affiche après le montant (« 40.00 TND »).

---

## 1. Auth

| # | Méthode | Chemin | Appelé par | Payload (cible) | Réponse (cible) | Statut | Écart / notes |
|---|---|---|---|---|---|---|---|
| A1 | POST | `/api/auth/login` | `LoginScreen` → `AuthContext.login` | `{ email, password }` (public) | `{ accessToken, refreshToken }` ; le mobile enchaîne sur A3 pour le profil | **EXISTE** | Jetons D-12 / D-23 depuis 4.4 : `accessToken` 1 h et `refreshToken` 30 j, claim `type`, deux secrets. Le mobile stocke la paire et rejoue sur 401 via `/refresh` (4.5), puis appelle A3 (6.2). |
| A2 | POST | `/api/auth/register` | `RegisterScreen`, `InstructorRegistrationScreen` (une seule étape depuis 6.1) | `{ email, password, firstName, lastName, schoolCode?, phone?, licenseNumber?, dateOfBirth?, address?, emergencyContact?, emergencyPhone? }` (public). Sans `schoolCode` → rôle `student`. Avec `schoolCode` valide → rôle = `school_codes.role`, `phone` et `licenseNumber` **requis**, ligne `instructors` créée, code consommé (D-17). Le champ `role` n'est **plus accepté**. | 201 `{ accessToken, refreshToken }` ; 400 `INVALID_SCHOOL_CODE` si code inconnu/expiré/épuisé ; 409 `CONFLICT` si email pris | **EXISTE** | Backend conforme (4.1, 4.2) : `role` refusé (400) ; avec `schoolCode`, `phone` et `licenseNumber` exigés par Joi, code consommé et fiche `instructors` créée dans une transaction (`uses_count` incrémenté en une seule instruction : deux inscriptions simultanées ne dépassent pas `max_uses`). Mobile conforme depuis 6.1 : `RegisterScreen` envoie `{ email, password, firstName, lastName }`, `InstructorRegistrationScreen` est un seul formulaire qui ajoute `phone`, `licenseNumber`, `schoolCode` ; plus de `role`. **Coordonnées facultatives (D-50, 12.1)** : `dateOfBirth` (date ISO `YYYY-MM-DD`, jamais dans le futur), `address` (≤ 500), `emergencyContact` (≤ 255), `emergencyPhone` (même format que `phone`) sont acceptées dans les deux cas et enregistrées sur le **compte** (`users`, migration 014) ; elles ne sont jamais exigées. Une école qui approuve l'inscription les recopie dans la fiche élève (E5, 12.2). Pas de photo en v1 (D-49). |
| A3 | GET | `/api/auth/me` | `AuthContext` après A1 / A2 et au démarrage de l'app ; plus aucun middleware backend après 2.2 | — | `{ id, email, firstName, lastName, role, createdAt }` + si `role = instructor` : `schoolId`, `instructorId` (D-19) | **EXISTE** | Format du contrat (5.1) ; `schoolId` / `instructorId` par la fiche `instructors` du compte (absents si un instructeur n'a pas de fiche). Mobile (6.2) : `AuthContext` stocke `{ id, email, firstName, lastName, role, schoolId?, instructorId? }` après A1 / A2 et le rafraîchit au démarrage (copie locale conservée si le réseau manque). |
| A4 | POST | `/api/auth/refresh` | `ApiClient` interceptor sur 401 (4.5 : un seul refresh partagé, rejeu, déconnexion si refus) | `{ refreshToken }` (public) | `{ accessToken, refreshToken }` — **nouveau** refresh, l'ancien est révoqué (rotation, D-12) | **EXISTE** | Rotation (4.6) : le refresh token est consommé en Redis (GETDEL) et une paire de la même session (`sid`) est émise ; un refresh token valide mais déjà consommé → 401 et révocation de toute la session (marqueur Redis 30 j) ; une session révoquée refuse tous ses refresh tokens. Un access token présenté ici → 401 (claim `type`). |
| A5 | POST | `/api/auth/logout` | `AuthContext.logout` | — (Bearer) | 204 ; le refresh token courant est révoqué | **EXISTE** | Révoque la session du jeton présenté (`sid` de l'access token → marqueur Redis) : ses refresh tokens sont refusés ; l'access token courant reste valable jusqu'à son expiration (1 h max, accepté). Mobile (6.2) : appelé à la déconnexion, puis le stockage local est effacé même si l'appel échoue. |

## 2. Écoles

| # | Méthode | Chemin | Appelé par | Payload (cible) | Réponse (cible) | Statut | Écart / notes |
|---|---|---|---|---|---|---|---|
| S1 | GET | `/api/schools` | `SchoolsListScreen` | — (public) | `School[]` : `{ id, name, address, phone, email, logoUrl?, currency, createdAt }` | **EXISTE** | `currency` = code ISO 4217 de l'école (D-43, migration 012, `TND` par défaut ; modifiable par les routes admin `POST /` et `PUT /:id`, ou `CURRENCY=` du script d'onboarding). Mobile aligné (6.1, 7.1) : `School` de `src/models/School.ts` = cet objet. |
| S2 | GET | `/api/schools/:id` | `SchoolDetailScreen`, `useSchoolCurrency` (tout écran qui affiche un montant) | — (public) | `School` | **EXISTE** | Le mobile lit ici la devise d'une école, une fois par session (7.1). |
| S3 | GET | `/api/schools/:id/instructors` | `SchoolDetailScreen` | — (public) | `Instructor[]` : `{ id, userId, schoolId, firstName, lastName, phone, licenseNumber, specialties[] }` | **EXISTE** | `firstName` / `lastName` par jointure `users` (D-16, 3.1 ; `''` si aucun compte lié). Le champ dérivé `name` a été retiré du payload en 6.1 (la colonne héritée `instructors.name` n'est plus lue). L'`id` est `instructors.id`, réutilisé comme `preferredInstructorId` dans L2. |
| S4 | GET | `/api/schools/:id/pricing` | `SchoolDetailScreen` (onglet Tarifs) | — (public) | `Pricing[]` : `{ id, schoolId, lessonType ∈ CODE\|Manœuvre\|Parc, price, duration }` | **EXISTE** | — |
| S5 | POST | `/api/schools/school-codes/verify` | — | — | — | **SUPPRIMÉE** | Remplacée par `schoolCode` dans A2 (D-17). `SchoolCodeService.ts` et le chemin ont été retirés du mobile en 6.1 ; l'écran d'inscription instructeur n'a plus qu'une étape. |
| S6 | GET | `/api/schools/:id/students` | `BookForStudentScreen` | — (instructeur de cette école ou admin) | `[{ studentId (users.id), firstName, lastName, email, phone?, enrollmentDate, completedLessons }]` — élèves `authorized` de l'école | **EXISTE** | Instructeur de cette école (403 `FORBIDDEN_SCHOOL` sinon) ou admin ; 404 école inconnue ; triée par nom (5.1). Mobile (6.4) : `SchoolService.getSchoolStudents`, liste filtrable par nom ou email dans `BookForStudentScreen`. |
| S7 | PUT | `/api/schools/:id` | `MySchoolScreen` (12.5) | `{ name?, address?, phone?, email?, currency? }` — au moins un champ (`instructor` de cette école / `admin`) | `School` mise à jour | **EXISTE** | Ouverte à l'instructeur de l'école en 12.4 (D-51) : l'école reste **créée** par l'administrateur, mais son instructeur corrige la fiche depuis l'application. Cloisonnée par `SchoolGuard.assertSameSchool` (403 `FORBIDDEN_SCHOOL` sur une autre école, D-20) ; 404 école inconnue ; corps vide → 400. `currency` = code ISO 4217 en majuscules (D-43). |
| S8 | POST | `/api/schools/:schoolId/pricing` | `MySchoolScreen` (12.5) | `{ lessonType ∈ CODE\|Manœuvre\|Parc, price > 0, duration > 0 }` (`instructor` de cette école / `admin`) | 201 `Pricing` — **upsert** sur (école × type) | **EXISTE** | Ouverte à l'instructeur en 12.4 (D-51), même cloisonnement que S7. Un second appel sur le même type remplace le tarif au lieu d'en créer un deuxième. La lecture (S4) reste publique. |
| S9 | DELETE | `/api/schools/pricing/:id` | `MySchoolScreen` (12.5) | — (`instructor` de l'école du tarif / `admin`) | 204 | **EXISTE** | Ouverte à l'instructeur en 12.4 (D-51) : le tarif désigne son école, contrôlée avant la suppression (403 `FORBIDDEN_SCHOOL`) ; 404 `NOT_FOUND` sur un tarif inconnu. Une leçon déjà planifiée garde son prix figé (D-30). |

## 3. Inscriptions (enrollment)

`:schoolId` = `schools.id`, `:requestId` = `enrollment_requests.id`, `studentId` = `users.id`.

| # | Méthode | Chemin | Appelé par | Payload (cible) | Réponse (cible) | Statut | Écart / notes |
|---|---|---|---|---|---|---|---|
| E1 | GET | `/api/enrollment/schools/:schoolId/status` | `SchoolDetailScreen`, `BookLessonScreen` | — (`student`) | `{ isEnrolled, requestStatus?, enrollmentDate?, canBook }` | **EXISTE** | — |
| E2 | POST | `/api/enrollment/schools/:schoolId/request` | `SchoolDetailScreen` (modal) | `{ message }` (`student`) ; refusé 409 si une inscription ou une demande `pending`/`approved` existe **dans n'importe quelle école** (D-22) | 201 `EnrollmentRequest` : `{ id, studentId, schoolId, status, message, createdAt, updatedAt }` | **EXISTE** | 409 `CONFLICT` si l'élève a une ligne `students` ou une demande `pending`/`approved` dans n'importe quelle école (D-22, 3.5 ; index uniques de 009, une course simultanée donne aussi 409). Une nouvelle demande après un refus est acceptée, même école comprise. Joi : `message` optionnel ≤ 500 ; l'écran l'exige non vide. |
| E3 | GET | `/api/enrollment/my-requests` | `MyEnrollmentRequestsScreen` | — (`student`) | `EnrollmentRequest[]` + `schoolName` | **EXISTE** | — |
| E4 | GET | `/api/enrollment/schools/:schoolId/requests?status=` | `EnrollmentRequestsScreen` | query `status?` (`instructor` de cette école / `admin`) | `EnrollmentRequest[]` + `studentEmail`, `studentFirstName`, `studentLastName` | **EXISTE** | Cloisonné à l'école de l'instructeur (403 `FORBIDDEN_SCHOOL`, 5.1) ; identité de l'élève par jointure `users` (3.1), affichée par l'écran depuis 6.1. `InstructorDashboard` passe le `schoolId` de A3 à l'écran (6.2). |
| E5 | PUT | `/api/enrollment/:requestId/approve` | `EnrollmentRequestsScreen` | — (`instructor` de cette école / `admin`) | `EnrollmentRequest` (`approved`) ; crée `students` (`authorized = true`) dans la même transaction | **EXISTE** | Transaction (3.2), cloisonné à l'école de la demande (403 `FORBIDDEN_SCHOOL`, 5.1). |
| E6 | PUT | `/api/enrollment/:requestId/reject` | `EnrollmentRequestsScreen` (modal) | `{ reason }` — **10 à 500 caractères** (D-29) | `EnrollmentRequest` (`rejected`, `rejectionReason`) | **EXISTE** | Cloisonné à l'école de la demande (5.1). Le mobile valide 10–500 caractères avant envoi, bouton désactivé sinon (6.2). |

## 4. Leçons

Modèle cible (D-01, D-21, D-32) : **une seule table `lessons`**. Une leçon naît d'une **demande** de l'élève (`status = pending`, `requestedDate`, `type`, `notes`, et un `preferredInstructorId` **facultatif** qui n'est qu'une préférence), **adressée à l'école, pas à un instructeur** ; elle n'est possible que si l'élève a une inscription **approuvée** dans cette école. **L'instructeur qui approuve devient l'instructeur de la leçon** (`instructorId` renseigné à ce moment), en fixant `scheduledDate` (→ `scheduled`, prix copié de la grille ou saisi à la main s'il n'y a pas de tarif, D-30) ; ou il la **rejette** (→ `rejected`, `rejectionReason`) ; la présence la passe en `completed` ; l'annulation en `cancelled` (règle 24 h, D-24, contrôlée **côté serveur**, le masquage du bouton n'est que du confort). **Toutes les leçons sont individuelles en v1, `CODE` compris** (`capacity` conservée, forcée à 1 — D-34) : une demande = une leçon = un élève ; douze élèves approuvés pour le même créneau de code donnent douze leçons, chacune avec sa présence. L'écran d'approbation offre une sélection multiple des demandes de code (6.7, `LessonService.approveLessons`) qui appelle L5 une fois par demande, en séquence, avec récapitulatif des succès et des échecs.

Objet `Lesson` (cible) : `{ id, schoolId, preferredInstructorId?, instructorId?, instructor?: { id, firstName, lastName }, studentId (users.id), student: { id, firstName, lastName }, type, status, requestedDate?, scheduledDate?, durationMinutes?, price?, capacity, currentBookings, notes?, adminNotes?, rejectionReason?, cancellationReason?, cancelledBy? (users.id), attended?, feedback?, rating?, paid, amount?, paymentDate?, paymentMethod?, creditApplied, createdAt, updatedAt }`. `instructorId` / `instructor` sont `null` tant que la leçon est `pending`. Paiement (D-40) : `amount` = argent versé pour la leçon, `creditApplied` = part du prix couverte par l'avoir de l'élève à la planification (`paymentMethod = 'credit'` et `amount = 0` quand l'avoir couvre tout ; `amount` = reste dû, `paid = false` quand il ne couvre qu'une partie).

| # | Méthode | Chemin | Appelé par | Payload (cible) | Réponse (cible) | Statut | Écart / notes |
|---|---|---|---|---|---|---|---|
| L1 | GET | `/api/lessons` | `MyLessonsScreen` (élève), `LessonRequestsScreen`, `TodayLessonsScreen` (instructeur) | query optionnelle : **`status`** ∈ `pending\|scheduled\|completed\|cancelled\|rejected` (plusieurs valeurs séparées par des virgules) ; **`scope`** ∈ `school\|mine` — instructeur seulement : `school` = les demandes `pending` de son école (file partagée), `mine` = les leçons dont il est `instructorId` ; sans `scope` = les deux ; ignoré pour un élève ; **`date`** = un jour `YYYY-MM-DD`, filtre sur `scheduledDate` (et `requestedDate` pour les `pending`) dans le fuseau de l'école | `Lesson[]` **de l'appelant**, triées par date croissante : élève → ses leçons (avec `paid`, `amount`) ; instructeur → selon `scope`. `LessonRequestsScreen` = `?status=pending&scope=school` ; `TodayLessonsScreen` = `?status=scheduled&scope=mine&date=<aujourd'hui>` → il ne voit et ne pointe que **ses** leçons | **EXISTE** | Scoping par l'appelant (5.2) : élève → ses leçons ; instructeur → `scope=school` (demandes `pending` de son école), `scope=mine` (ses leçons), sans `scope` les deux ; admin → tout. `date` = jour dans le fuseau `Africa/Tunis` (une seule zone en v1, les écoles n'en ont pas). Mobile : `Lesson` de `src/models/Lesson.ts` = cet objet, `LessonService.getMyLessons(filters?)` (6.1, 6.3) ; `MyLessonsScreen` affiche `paid` / `amount` ; `LessonRequestsScreen` = `?status=pending&scope=school`, `TodayLessonsScreen` = `?status=scheduled,completed&scope=mine&date=<jour local>` (l'onglet Completed montre les leçons déjà pointées). |
| L2 | POST | `/api/lessons` | `BookLessonScreen` (élève) | `{ type ∈ CODE\|Manœuvre\|Parc, requestedDate (ISO, futur), preferredInstructorId?, notes? }` (`student` ; **refusé 403 `NOT_ENROLLED` si l'élève n'a pas d'inscription `approved`** ; école résolue depuis cette inscription ; si `preferredInstructorId` est fourni, il doit être de cette école) | 201 `Lesson` (`pending`, `instructorId: null`) | **EXISTE** | Conforme (5.2) : 403 `NOT_ENROLLED` sans fiche `students` autorisée, école résolue depuis cette fiche (D-22), instructeur préféré d'une autre école → 400 `VALIDATION_ERROR`. Mobile conforme depuis 6.1 : `BookLessonScreen` envoie `{ type, requestedDate, preferredInstructorId?, notes? }` — sélecteur à 3 types, date et heure souhaitées, instructeur facultatif (préférence posée depuis la fiche école, effaçable). |
| L3 | POST | `/api/lessons/:id/cancel` | `MyLessonsScreen` (élève), écrans instructeur | `{ reason? }` — **deux chemins distincts selon le rôle de l'appelant** : **(1) élève** : uniquement sa propre leçon ; `pending` → toujours ; `scheduled` → seulement si `scheduledDate − now ≥ 24 h` (`LESSON_CANCEL_HOURS`, D-24), sinon 403 `CANCEL_WINDOW_CLOSED` ; **(2) instructeur** : n'importe quelle leçon de son école (`pending` ou `scheduled`, la sienne ou celle d'un collègue), **à tout moment, sans fenêtre** ; hors de son école → 403 `FORBIDDEN_SCHOOL` | `Lesson` (`cancelled`) ; **le contrôle serveur est la règle**, le mobile masque le bouton en plus (`canStudentCancel`, 6.6) et affiche le refus `CANCEL_WINDOW_CLOSED` avec le message du serveur. Leçon déjà payée ou réglée par l'avoir : le versement (`amount`) et le crédit consommé (`creditApplied`) reviennent à l'avoir de l'élève (**D-40**), dans la même transaction ; la leçon annulée garde sa trace de paiement | **EXISTE** | Conforme (5.3, 7.2) : `reason` et l'auteur sont conservés (`cancellationReason`, `cancelledBy` = users.id, migration 011) ; statut autre que `pending` / `scheduled` → 409 `CONFLICT` ; l'admin annule comme un instructeur ; avoir crédité (`students.credit`, migration 013). Mobile : « Your payment was returned to your credit ». |
| L4 | POST | `/api/lessons/book-for-student` | `BookForStudentScreen` (instructeur) | `{ studentId (users.id, choisi dans S6, inscription `approved` requise), type, scheduledDate, durationMinutes, price?, notes? }` | 201 `Lesson` (`scheduled`, `instructorId` = appelant, prix de la grille ou saisi ; 400 `PRICE_REQUIRED` si aucun ; avoir de l'élève imputé comme en L5, D-40) | **EXISTE** | Conforme (5.4) : instructeur seulement, élève résolu par `users.id` avec fiche `students` autorisée dans l'école de l'instructeur (403 `NOT_ENROLLED` sinon), prix figé comme en L5. Mobile conforme depuis 6.4 : l'élève est choisi dans la liste S6 (`studentId` = users.id), durée pré-remplie depuis la grille S4, champ prix seulement sans tarif (D-30). |
| L5 | PUT | `/api/lessons/:id/approve` | `LessonRequestsScreen` | `{ scheduledDate (ISO, futur), durationMinutes, price?, adminNotes? }` — **tout instructeur de l'école** ; celui qui approuve devient `instructorId` | `Lesson` (`scheduled`, `instructorId` = appelant, `price` = `pricing[school, type]` si un tarif existe, sinon le `price` saisi ; **400 `PRICE_REQUIRED`** si ni l'un ni l'autre). Le montant est stocké sur la leçon dans les deux cas (D-30). L'avoir de l'élève est imputé dans la même transaction (D-40) : `creditApplied`, et `paid` / `paymentMethod = 'credit'` s'il couvre le prix | **EXISTE** | Conforme (5.3) : instructeur seulement (l'admin n'a pas de fiche `instructors` à devenir l'instructeur), 403 `FORBIDDEN_SCHOOL` hors de son école, 409 `CONFLICT` si la demande n'est plus `pending` (course entre collègues incluse). Mobile (6.3, 6.7) : `LessonRequestsScreen` propose la date souhaitée de l'élève, la durée de la grille (60 min sinon), et n'affiche le champ prix que si S4 n'a pas de tarif pour le type (D-30) ; 409 / `PRICE_REQUIRED` affichés depuis `message`. Les demandes `CODE` cochées se planifient ensemble pour un même créneau : un L5 par demande, en séquence (D-34). |
| L6 | PUT | `/api/lessons/:id/reject` | `LessonRequestsScreen` | `{ reason }` (10–500 car., D-29) — tout instructeur de l'école | `Lesson` (`rejected`, `rejectionReason`) | **EXISTE** | Conforme (5.3) : instructeur de l'école ou admin ; 409 `CONFLICT` si plus `pending`. Mobile (6.3) : motif 10–500 caractères validé avant envoi, bouton désactivé sinon. |
| L7 | PUT | `/api/lessons/:id/attendance` | `TodayLessonsScreen` | `{ attended, feedback?, rating? }` par **identifiant de leçon** (une leçon = un élève, D-34) — **uniquement l'instructeur de la leçon** (`instructorId` = appelant, sinon 403) | `Lesson` (`completed`) ; `student_lesson_stats.completed_lessons` **incrémenté seulement si `attended = true`**. Absence (`attended = false`, **D-41**) : la leçon n'est pas facturée — hors du dû (P4), P6 la refuse — et ce que l'élève avait déjà versé ou réglé par l'avoir revient à son avoir (comme une annulation, D-40) | **EXISTE** | Conforme (5.4, 7.3) : 409 `CONFLICT` si la leçon n'est pas `scheduled`, 403 `FORBIDDEN` si l'appelant n'en est pas l'instructeur ; leçon, compteur (`student_lesson_stats`, présent seulement — D-33) et avoir (absence prépayée) écrits dans une même transaction. Mobile (6.3) : présent / absent, note 1–5 (présent seulement) et commentaire facultatifs ; « Absent — not billed » sur les historiques (7.3). |
| L8 | GET | `/api/lessons/instructors` | Aucun écran actif | — | — | **SUPPRIMÉE** | Les instructeurs sont sous S3. Retirée de `api.config.ts` / `LessonService` en 6.1. |

## 5. Examens

Modèle cible (D-01) : un examen est **une demande d'élève** `{ type, preferredDate, message }` (`pending`), adressée à l'école — **pas d'instructeur attitré** ; tout instructeur de l'école la voit et agit dessus. L'instructeur la **planifie** (`dateTime`, `location` → `scheduled`), la **rejette** (`rejectionReason` → **`rejected`**, même vocabulaire que les leçons ; `cancelled` est réservé à une annulation après planification), puis enregistre `{ result, score?, notes? }` (→ `completed`). **Pas de règle d'éligibilité** (D-26) ; le nombre de leçons complétées de l'élève est **renvoyé dans chaque `Exam`** (`studentCompletedLessons`) pour aider l'instructeur à décider. Résultats enregistrés par l'**instructeur** (D-20). **Procédure selon le type (D-42)** : pour la **théorie**, l'école choisit la date — « Schedule » fixe un rendez-vous, « Reject » signifie que l'école ne présente pas l'élève ; pour la **pratique**, la date est imposée par la session ATTT — « Record convocation » enregistre la date et le centre reçus, « File not ready » signifie que le dossier n'est pas prêt et que l'élève redemandera à la session suivante. Mêmes routes, mêmes payloads, mêmes statuts (`scheduled` / `rejected`) ; seuls les mots des écrans changent (`EXAM_PROCEDURES`, `mobile-app/src/models/Exam.ts`).

Objet `Exam` (cible) : `{ id, schoolId, studentId (users.id), studentFirstName, studentLastName, studentCompletedLessons, type ∈ theory\|practical, status ∈ pending\|scheduled\|completed\|cancelled\|rejected, preferredDate?, message?, dateTime?, location?, result ∈ pending\|passed\|failed, score?, notes?, rejectionReason?, paid, amount?, paymentDate?, createdAt, updatedAt }`.

| # | Méthode | Chemin | Appelé par | Payload (cible) | Réponse (cible) | Statut | Écart / notes |
|---|---|---|---|---|---|---|---|
| X1 | GET | `/api/exams/my-exams` | `MyExamsScreen` (élève), `ExamRequestsScreen`, `TodayExamsScreen` (instructeur) | query optionnelle `status` | `Exam[]` **de l'appelant** : élève → les siens (avec `paid`, `amount`) ; instructeur → **tous ceux de son école** (pas d'instructeur attitré), avec `studentCompletedLessons` | **EXISTE** | Conforme (5.5) : élève → ses examens ; instructeur → tous ceux de son école (`studentFirstName`, `studentLastName`, `studentCompletedLessons` joints) ; admin → tout ; `status` accepte plusieurs valeurs séparées par des virgules. Mobile aligné (6.1, 6.5) : `Exam` de `src/models/Exam.ts` = cet objet, `ExamService.getMyExams(filters?)` ; `MyExamsScreen` affiche `rejected` (motif) et `paid` / `amount` ; `ExamRequestsScreen` = `?status=pending`, `TodayExamsScreen` = `?status=scheduled,completed` filtré sur le jour local (X1 n'a pas de filtre de date). |
| X2 | POST | `/api/exams/request` | `RequestExamScreen` (élève) | `{ examType ∈ theory\|practical, preferredDate (ISO, futur), message? }` (`student` ; **refusé 403 `NOT_ENROLLED` si l'élève n'a pas d'inscription `approved`**, même règle que L2 ; école résolue depuis cette inscription, D-22) | 201 `Exam` (`pending`) | **EXISTE** | Conforme (5.5) : 403 `NOT_ENROLLED` sans fiche `students` autorisée, école résolue depuis cette fiche (D-22), aucune règle d'éligibilité (D-26). |
| X3 | PUT | `/api/exams/:id/schedule` | `ExamRequestsScreen` | `{ dateTime (ISO, futur), location }` — instructeur de l'école | `Exam` (`scheduled`) | **EXISTE** | Conforme (5.6) : instructeur de l'école ou admin, 409 `CONFLICT` si la demande n'est plus `pending`. Mobile (6.5, 7.4) : date et heure pré-remplies avec la date souhaitée, centre obligatoire ; libellés selon le type (D-42) : « Schedule » / « Exam date » / « Location » pour la théorie, « Record convocation » / « Session date (ATTT) » / « Exam center (ATTT) » pour la pratique ; l'élève voit « Scheduled » ou « Convocation received ». |
| X4 | PUT | `/api/exams/:id/reject` | `ExamRequestsScreen` | `{ reason }` (10–500 car.) | `Exam` (**`rejected`**, `rejectionReason`) | **EXISTE** | Conforme (5.6) : instructeur de l'école ou admin, 409 si plus `pending`. Mobile (6.5, 7.4) : motif 10–500 caractères validé avant envoi ; libellé selon le type (D-42) : « Reject » pour la théorie, « File not ready » pour la pratique (l'élève lit « File not ready — you can request again for the next session »). |
| X5 | PUT | `/api/exams/:id/result` | `TodayExamsScreen` | `{ result ∈ passed\|failed, score? (0–100, **facultatif** : un examen de conduite est admis/ajourné sans note, seul le code donne un score), notes? }` — **instructeur** de l'école (D-20) | `Exam` (`completed`) | **EXISTE** | Conforme (5.6) : instructeur de l'école ou admin, seulement sur un examen `scheduled` (409 sinon), score facultatif. Mobile (6.5) : `TodayExamsScreen` enregistre `passed` / `failed`, score facultatif (entier 0–100 s'il est saisi), notes facultatives. |

## 6. Profils (fiche élève)

Deux familles montées par le module `student` de `services/api` : `/api/profiles/*` (vue instructeur) et `/api/student-profiles/*` (vue élève), **joignables via Nginx depuis 2.6**. **`:studentId` = `users.id`** (D-28) ; le backend joint `students ON user_id = :studentId AND school_id = :schoolId`.

| # | Méthode | Chemin | Appelé par | Payload | Réponse (cible) | Statut | Écart / notes |
|---|---|---|---|---|---|---|---|
| P1 | GET | `/api/profiles/:studentId/schools/:schoolId/complete` | `StudentInfoTab` (écran `StudentProfile`, joint depuis `EnrollmentRequests` (demandes approuvées) et `BookForStudent` (liste S6) — 6.6) | — (`instructor` de cette école / `admin`) | `StudentProfile` `{ id (users.id), firstName, lastName, email, phone?, address?, dateOfBirth?, licenseNumber?, enrollmentDate?, emergencyContact?, emergencyPhone?, totalLessons, completedLessons, completedLessonsByType: { CODE, Manœuvre, Parc }, totalExams, passedExams, notes? }` | **EXISTE** | `users.id` en entrée, fiche résolue par `user_id` + `school_id` (D-28, 5.0) ; P1–P4 cloisonnées à l'école de l'instructeur (403 `FORBIDDEN_SCHOOL`, 5.1) ; `totalLessons` / `totalExams` = leçons / examens planifiés ou passés (les demandes en attente, refusées ou annulées ne comptent pas) ; `completedLessons` = `student_lesson_stats` (L7) ; `completedLessonsByType` (8.1, D-45) = leçons `completed` avec présence (`attended = true`) par type, comptées dans `lessons` — même base que `completedLessons`, sans objectif ni seuil (D-26). 404 sans fiche dans cette école. |
| P2 | GET | `/api/profiles/:studentId/schools/:schoolId/lessons` | `StudentLessonsTab` | — | `LessonHistory[]` `{ id (lesson), type, status, scheduledDate, durationMinutes, instructorFirstName, instructorLastName, attended?, feedback?, rating?, paid, price?, amount?, paymentDate?, paymentMethod?, creditApplied }` | **EXISTE** | Source `lessons` (007) : leçons `scheduled`, `completed` et `cancelled` de l'élève dans l'école, les plus récentes d'abord ; `status` et `price` ajoutés (5.0), `creditApplied` (7.2). |
| P3 | GET | `/api/profiles/:studentId/schools/:schoolId/exams` | `StudentExamsTab` | — | `ExamHistory[]` `{ id (exam), type, status, dateTime, location?, result, score?, notes?, paid, price?, amount?, paymentDate?, paymentMethod? }` | **EXISTE** | Source `exams` (008) : examens `scheduled`, `completed` et `cancelled` ; `status`, `location`, `price` ajoutés (5.0). |
| P4 | GET | `/api/profiles/:studentId/schools/:schoolId/financial` | `StudentInfoTab` | — | `FinancialSummary` `{ totalRevenue, totalPending, totalDue, lessonsRevenue, examsRevenue, lessonsPending, examsPending, lastPaymentDate?, credit }` | **EXISTE** | Encaissé = argent versé (`amount` des leçons / examens payés ; une leçon réglée par l'avoir vaut 0) ; dû = leçons / examens `scheduled` ou `completed` non payés, au montant saisi (reste après avoir) sinon au prix (5.0, 7.2) ; `credit` = avoir disponible de l'élève (D-40) ; une leçon `completed` avec `attended = false` n'est pas due (D-41, 7.3). |
| P5 | PUT | `/api/profiles/:studentId/notes` | `StudentInfoTab` | `{ notes }` — note privée instructeur, portée à l'école de l'appelant | 204 | **EXISTE** | `students.notes` résolu par `user_id` (une seule fiche par élève, D-22) ; 404 sans fiche ; 403 `FORBIDDEN_SCHOOL` si la fiche est dans une autre école que l'instructeur (5.1). |
| P6 | PUT | `/api/profiles/lessons/:lessonId/mark-paid` | `StudentLessonsTab` | `{ amount, paymentMethod ∈ cash\|card\|bank_transfer }` | 204 ; **409 `CONFLICT`** si la leçon est une absence (`attended = false`, D-41) | **EXISTE** | `lessons.paid / amount / payment_method / payment_date` (5.0) ; 404 si la leçon n'existe pas, 403 `FORBIDDEN_SCHOOL` si elle est dans une autre école (5.1), 409 sur une absence (7.3). L'ancien chemin `/bookings/:bookingId/mark-paid` a disparu ; le mobile appelle ce chemin depuis 6.1 et masque « Mark as Paid » sur une absence. |
| P7 | PUT | `/api/profiles/exams/:examId/mark-paid` | `StudentExamsTab` | `{ amount, paymentMethod }` | 204 | **EXISTE** | `exams.paid / amount / payment_method / payment_date` (5.0) ; 404 si l'examen n'existe pas, 403 `FORBIDDEN_SCHOOL` si il est dans une autre école (5.1). L'ancien chemin `/registrations/:registrationId/mark-paid` a disparu ; le mobile appelle ce chemin depuis 6.1. |
| P8 | GET | `/api/student-profiles/me/schools/:schoolId/profile` | `MyProgressTab` (écran `MyProfile`, joint depuis `StudentDashboard` avec l'école de l'inscription approuvée trouvée par E3 — 6.6) ; `StudentDashboard` (parcours, 8.2) | — (`student`) | `MyProfile` (= P1 sans `notes`, `completedLessonsByType` compris) | **EXISTE** | `users.id` du jeton (D-28, 5.0) ; 404 si l'élève n'a pas de fiche dans cette école. |
| P9 | GET | `/api/student-profiles/me/schools/:schoolId/lessons` | `MyLessonsPaymentTab` | — | `LessonHistory[]` (sans `feedback` privé si décidé plus tard) | **EXISTE** | Idem P2 (5.0). |
| P10 | GET | `/api/student-profiles/me/schools/:schoolId/exams` | `MyExamsPaymentTab` | — | `ExamHistory[]` | **EXISTE** | Idem P3 (5.0). |
| P11 | GET | `/api/student-profiles/me/schools/:schoolId/financial` | `MyProgressTab` | — | `FinancialSummary` | **EXISTE** | Idem P4 (5.0). |

## 7. Notifications

**Hors v1 (D-35).** Aucune route, aucune table, aucun code mobile. Le module `services/notification` n'est pas porté dans `services/api` ; son code reste dans l'historique git. L'état d'une demande (inscription, leçon, examen) est visible sur le tableau de bord de chaque rôle via E3, L1 et X1.

## 8. Routes backend hors contrat mobile

État après 5.7 : toute route absente des §1–7 est soit supprimée, soit listée ici derrière `admin`. Aucun écran ne les appelle.

- `/api/verification/*` : **supprimé en 5.7** (routes, controller et service ; `student_lesson_stats` est mis à jour directement par L7). Répond 404 comme toute route inconnue.
- `/api/schools` : `POST /`, `DELETE /:id`, `POST /:schoolId/instructors`, `GET /instructors/:id` (verrouillée `admin` en 5.7, elle était publique), `PUT|DELETE /instructors/:id` — **conservées, `admin` uniquement** (onboarding des écoles par script + ces routes). `PUT /:id`, `POST /:schoolId/pricing` et `DELETE /pricing/:id` **ont quitté cette liste en 12.4** : elles sont entrées au contrat mobile (S7, S8, S9) et acceptent l'instructeur de l'école concernée (D-51).
- `/api/lessons` : `GET /:id` (conservée : sa propre leçon pour un élève, 403 `FORBIDDEN` sinon ; leçons de son école pour un instructeur, D-20 ; tout pour un admin). **Retirées en 5.2** avec les anciennes routes de créneaux (`lesson_bookings` n'est plus lue) : `GET /:id/availability`, `PUT /:id`, `DELETE /:id`, `POST /:lessonId/book`, `GET /bookings/:id`, `GET /:lessonId/bookings`, `GET /students/:studentId/bookings`, `DELETE /bookings/:bookingId`, ancien `POST /` admin (remplacé par L2).
- `/api/exams` : `GET /:id` (conservée : son propre examen pour un élève, 403 `FORBIDDEN` sinon ; examens de son école pour un instructeur, D-20 ; tout pour un admin). **Retirées en 5.5** (anciennes sessions) : `POST /`, `GET /`, `PUT /:id`, `DELETE /:id`. **Retirées en 3.4** avec la table `exam_registrations` (D-26) : `GET /:id/availability`, `POST /:examId/register`, `GET /registrations/:id`, `GET /:examId/registrations`, `GET /students/:studentId/registrations`, `GET /students/:studentId/eligibility`, `PUT /registrations/:id/result`, `DELETE /registrations/:id`.
- `/api/payments/*` : module porté, **non monté** (D-31).
- `/api/analytics/*` : **supprimé** (D-31).

## 9. Chemins définis côté mobile mais appelés par aucun écran actif — **supprimés en 6.1**

`src/config/api.config.ts` ne définit plus que les chemins des §1–6 ; les `*Service.ts` n'ont plus que les méthodes des lignes ci-dessus. Retirés en 6.1 : `GET /api/lessons/requests`, `GET /api/lessons/today`, `GET /api/lessons/instructors` (L8), `GET /api/exams/requests`, `GET /api/exams/today`, `POST /api/schools/school-codes/verify` et `GET /api/schools/:id/codes` (S5, `SchoolCodeService.ts` supprimé), `POST/PUT/DELETE /api/schools…` (`createSchool` / `updateSchool` / `deleteSchool`), `/api/verification/*`, `/api/payments`, `/api/notifications`. Écrans morts supprimés en 0.8 : `StudentDashboardScreen.tsx`, `RequestLessonScreen.tsx`, `ExamsListScreen.tsx`.

## 10. Synthèse

| Statut | Nombre | Lignes |
|---|---|---|
| EXISTE | 39 | A1–A5, S1–S4, S6, E1–E6, L1–L7, X1–X5, P1–P11 |
| DIVERGE | 0 | — |
| MANQUE | 0 | — |
| SUPPRIMÉE | 2 | S5, L8 |

**Contrat entièrement EXISTE depuis la tâche 5.8** : les 39 routes des §1–7 sont implémentées et couvertes par les tests de bout en bout (`tests/e2e/*`, bloquants en CI). Les colonnes « Écart / notes » ne décrivent plus que l'état du mobile (Phase 6) et les points suspendus.

Décisions du 19/09/2026 appliquées en Phase 7 : L3 / L4 / L5 / P4 (D-40, 7.2) ; L7 / P4 / P6 (D-41, 7.3) ; X3 / X4 libellés (D-42, 7.4) ; S1 / S2 (D-43, 7.1).
