#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# --config : désactive la découverte de configs, qui parcourt tous les fichiers suivis
# (dont des node_modules encore indexés, voir tâche 0.2) et trébuche sur des configs invalides.
npx --no -- lint-staged --config package.json
