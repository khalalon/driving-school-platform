# Changelog

Format : une ligne par tâche livrée de `docs/PLAN.md`, sous la forme `- [x.y] description courte (commit)`. Section « Unreleased » tant qu'aucune version n'est taguée.

## Unreleased

- [16/09/2026] Réponses aux 16 questions ouvertes appliquées : D-16 à D-31 dans `docs/DECISIONS.md`, contrat et plan mis à jour. Restent Q-06b et Q-16b.
- [17/09/2026] 15 questions de comportement tranchées : D-32 (demande adressée à l'école, instructeur = préférence), D-33 (examens : `rejected`, score facultatif, pas d'instructeur attitré) ; contrat L1–L7, X1–X5 corrigé (`NOT_ENROLLED` sur L2 et X2, deux chemins d'annulation sur L3, filtres `status` / `scope` / `date` sur L1) ; Q-17, Q-18, Q-19 ouvertes.
- [17/09/2026] Q-06b → D-34 (leçons CODE individuelles, `capacity` forcée à 1, approbation multiple en 6.7) ; Q-16b → D-35 (pas de module notification en v1, tâche 3.3 supprimée, migrations 007–009 renumérotées). Restent Q-17, Q-18, Q-19.
- [0.1] Outillage réparé : migration `004_schema_migrations.sql` (table de suivi) et `scripts/migrate.sh` idempotent derrière `make migrate` (remplace `run-migrations.sh`) ; `student` dans `make install/test/lint/format/coverage`, `health` sur 3007 (student) et 3008 (analytics) ; stub `npm test` racine retiré, `pre-commit` → `lint-staged --config package.json` (Prettier sur les `.ts` des services) ; scopes commitlint `student`, `analytics`, `mobile`, `docs`, `infra`, `e2e`.
- [0.2] `.gitignore` racine (`node_modules/`, `dist/`, `coverage/`, `.env`, `.env.*.local`, `.DS_Store`, `*.log`, `.expo/`) ; `node_modules` et `services/student/node_modules` sortis de l'index (24 290 fichiers, disque intact) : 380 fichiers suivis.
- [0.3] `.env`, `mobile-app/.env` et `web-frontend/.env` sortis de l'index (aucun secret dedans : des URLs locales ; historique non purgé) ; `.env.example` racine complété (`CACHE_TTL`), `mobile-app/.env.example` mis à jour (port 80), `web-frontend/.env.example` créé (`VITE_API_BASE_URL`).
- [0.4] URL du backend mobile : une seule source (`app.json` → `expo.extra.API_BASE_URL`, surcharge `EXPO_PUBLIC_API_BASE_URL` via `mobile-app/.env`), plus aucune URL dans `api.config.ts` ; `expo-constants` déclaré en dépendance directe (il n'était que transitif, non hissé) ; typings `env.d.ts` réécrits (`@env` supprimé) ; `mobile-app/.env.example` réécrit ; `.env` racine sans variable mobile.
- [0.5] `services/auth/Dockerfile` : plus de fallback `|| (mkdir -p dist && cp -r src/* dist)`, une erreur `tsc` casse le build. Il masquait 3 erreurs de type réelles dans `auth`, corrigées type-only (`SignOptions['expiresIn']` dans `token.service.ts`, `RedisClientType` dans `redis.config.ts`) ; `tsc --noEmit` et 12/12 tests verts sur `auth`.
- [0.6] `npx tsc --noEmit` vert sur les 8 services (`npm ci` + typecheck, `OK 8/8`) : les 7 services autres qu'`auth` étaient déjà sans erreur, aucune modification de code (les 3 erreurs d'`auth` ont été corrigées en 0.5). Au passage : le script `prepare` de `services/analytics/package.json` détournait `core.hooksPath` vers un dossier sans hook à chaque `npm ci` — retiré.
- [18/09/2026] D-36 (enchaînement des tâches par phase, push à chaque commit, test sur téléphone Android), D-37 (seuils de couverture par service → tâche 0.9), D-38 (`app.json` valeur neutre `10.0.2.2` → tâche 0.8). Règle d'or 3 et règles de lecture du plan réécrites.
- [0.7] CI : `npx tsc --noEmit` avant `npm test` dans les 8 jobs de `ci-cd.yml`, jobs `student` et `analytics` ajoutés (détection de changements, tests, build d'images) ; `pr-check.yml` gagne un job `typecheck` en matrice sur les 8 services. Constat : `npm run lint` échoue partout (CRLF + quelques vraies erreurs) → tâche 0.10 ajoutée.
- [0.8] Fichiers morts supprimés : 8 scripts de test manuel à la racine, `scripts/test-api.sh` (+ cible `make api-test`), `mobile-app/src/app.ts` (serveur Express égaré), écrans `StudentDashboardScreen` / `RequestLessonScreen` / `ExamsListScreen`, `MOBILE_APP_COMPLETE.md`, `profile.service.ts` vide ; `app.json` → `API_BASE_URL` = `http://10.0.2.2:80` (D-38). `tsc` mobile : 39 erreurs restantes (30 dans `services/api`, 9 dans `AuthContext` et 4 écrans) → 6.1.
- [0.9] `npm test` vert sur les 8 services (`OK 8/8`) : 6 suites obsolètes supprimées (D-39, `passWithNoTests`), test analytics « cached data » en `it.skip` (D-31), seuils de couverture = couverture mesurée (D-37 : auth 16/21/17/16, analytics 34/28/30/35). Tests réécrits par module en Phase 2.
- [0.10] `npm run lint` vert sur les 8 services (`OK 8/8`) : `.gitattributes` (`* text=auto eol=lf`) élimine les `Delete ␍` de Windows ; ESLint des services legacy : `no-misused-promises` toléré pour les handlers Express, `no-unsafe-*`/`no-explicit-any`/`require-await` en `warn` (strict dans `services/api`, 2.1), `ignoreRestSiblings`, tests hors `tsconfig` ignorés ; 3 retouches de code sans effet ; `.prettierrc.json` + formatage pour `student` et `analytics`.
- [1.1] Harnais e2e : `tests/` devient un paquet npm (jest + ts-jest + supertest + pg), `globalSetup` attend la passerelle et applique `fixtures/seed.sql` idempotent (école, instructeur `instructor@seed.io`, grille, code `INST-SEED`, lecture de `POSTGRES_*` depuis le `.env` racine), helpers (`SEED`, `login`, `expectStatus`), auto-contrôle `harness.e2e.test.ts` (3/3 verts sur la stack), `npm run test:e2e` racine ; les deux anciens fichiers inexécutables supprimés.
- [1.2] `tests/e2e/critical-path.e2e.test.ts` : les 10 étapes du chemin critique D-15 sur les routes cibles (A2, E2, A1, E4, E5, E1, L2, L1, L5, L7), garde `step()` qui désigne la première étape en échec. Résultat aujourd'hui : `exit=1`, **échec à A2** (`{"error":"\"role\" is required"}`) — attendu jusqu'à 5.8.
- [1.3] `.github/workflows/e2e.yml` réécrit : stack Docker complète, attente de `/health`, `npm run test:e2e -- --ci --json --outputFile=report.json` en `continue-on-error` (`TODO(5.8)`), résumé par test dans le job et rapport JSON en artefact ; `test:e2e` racine transmet ses arguments à Jest. Piège documenté : Nginx garde l'IP d'un conteneur reconstruit (502) → `docker compose restart nginx`.
- [2.1] `services/api` créé : application Express unique (:3000, Node 20), `loadEnv` (Joi, secrets obligatoires), `http/errors.ts` (D-27 : `HttpError`, `sendError`, `sendCaughtError`, `sendValidationError`) + `http/validation.ts`, module `auth` porté en couches et typé strictement (lint `no-unsafe-*` en `error`), 35 tests (unitaires + HTTP supertest), seuils = mesure (72/58/66/72), service `api` dans le compose à côté des anciens. Scope commitlint `api`.
- [2.2] Middleware d'auth unique `services/api/src/middleware/auth.middleware.ts` : `authenticate(verifier)` (JWT vérifié localement, `req.user = { userId, email, role }`) et `authorize(...roles)` ; 8 tests HTTP (valide, absent, schéma, expiré, autre secret, mauvais rôle, bon rôle, authorize seul) ; types partagés `src/types/auth.ts` ; copie du module `auth` supprimée ; 43 tests, seuils 75/65/71/75.
- [2.3] Modules `school` et `student` portés dans `services/api` (routes → controllers → services → repositories, typés, `HttpError`) : S1–S4 + admin, E1–E6, P1–P11, `/api/verification` (tel quel). Corrections au passage : colonnes `schools`/`instructors`/`pricing` aliasées en camelCase, `LessonType` aligné sur `CODE/Manœuvre/Parc` (D-18, la base le refusait), payloads P5–P7 validés (Joi) et réponses 204 (cible). **Bug `userId` corrigé** : E2 via `services/api` renseigne `studentId` (l'ancien service met `null`). 115 tests (services, routes HTTP, repositories sur Pool factice), seuils 84/83/89/84.
- [2.4] Modules `lesson` et `exam` portés tels quels dans `services/api` (créneaux + réservations, sessions + inscriptions, éligibilité 20/30 conservée jusqu'à 5.7) ; `StudentRepository` du module student réutilisé (plus de SQL `students` dupliqué), `date-fns` non repris ; suppressions → 204 ; 158 tests, seuils 86/85/92/85.
- [2.5] `payment` porté dans `services/api` (typé, `HttpError`, passerelle factice sans `uuid`) mais **non monté** (D-31) ; `notification` et `analytics` non portés (D-31, D-35) ; `/api/payments`, `/api/notifications`, `/api/analytics/*` → 404. Constat : la table `payments` n'a pas de colonne `metadata` (bug préexistant, sans effet tant que le module est démonté). 169 tests, seuils 87/85/93/87.
- [2.6] Nginx : un seul `location /api/` vers `api:3000` (`include proxy_params.conf`, nom résolu à la requête via `resolver 127.0.0.11` → plus de 502 après recréation du conteneur), `/api/verification/*` → 404 JSON jamais transmis, plus de 443, 404 JSON par défaut ; `/api/profiles` et `/api/student-profiles` deviennent joignables. Les trois compose ne déclarent plus que `postgres`, `redis`, `api`, `nginx` (dev : API en `ts-node-dev` sur les sources ; prod : limites, journaux, pas de port direct) ; `.env.example` réduit aux variables lues (`REDIS_PASSWORD` et clés Stripe/Twilio/SendGrid retirées). Conteneurs des 8 anciens services retirés (`--remove-orphans`).
- [2.7] Les 8 anciens services supprimés (`services/` = `api` seul), ainsi que `scripts/quick-start.sh` (copie du schéma SQL hors migrations) et `scripts/docker-*.{sh,ps1}` (doublons cassés du Makefile). Makefile réécrit pour l'app unique (`install/typecheck/lint/test/format`, `dev/dev-watch/prod`, `health` sur :80 et :3000, `test-e2e`), CI : job `api` (Node 20) + image `driving-school-api`, `pr-check` typecheck `services/api` et `tests`, README et ARCHITECTURE réécrits, CLAUDE.md et contrat alignés. Chemin critique e2e : échoue toujours à A2 (= 1.2).
- [2.7 fix] Nginx écoute aussi en IPv6 (`listen [::]:80`) : le healthcheck interne (`wget localhost`) résolvait `::1` et échouait alors que la passerelle répondait ; conteneur `healthy`.
- [3.0] Migration `005_enrollment_student_not_null.sql` : 1 demande orpheline purgée, `enrollment_requests.student_id NOT NULL` ; appliquée par `scripts/migrate.sh`, idempotente.
- [3.1] Migration `006_user_names.sql` : `users.first_name` / `last_name` (backfill depuis `students.name` / `instructors.name`, `''` sinon, puis NOT NULL), `students.name` et `instructors.name` nullables. A2 exige `firstName` / `lastName` ; A3, E4 (`studentFirstName` / `studentLastName`), P1 (`firstName` / `lastName` remplacent `name`) et S3 (`firstName` / `lastName` + `name` dérivé transitoire) les exposent par jointure `users` ; `approveRequest` passe (INSERT `students` sans `name`). Harnais e2e : `helpers/flow.ts` (prérequis mémorisés, chaque étape exécutable seule via `-t`), seed avec noms. 173 tests unitaires.
- [3.2] `approveRequest` atomique : `src/db/transaction.ts` (`PgTransactionRunner`, type `Queryable`), `updateStatus` et `StudentRepository.create` acceptent le client de la transaction ; test avec client pg mocké dont l'INSERT rejette → ROLLBACK, jamais de COMMIT, demande toujours `pending`. 177 tests unitaires.
- [3.3] Migration `007_lessons_requests.sql` (D-21, D-32, D-34) : `lessons.student_id` → students NOT NULL, `instructor_id` nullable, `preferred_instructor_id`, `status` ∈ pending/scheduled/completed/cancelled/rejected (défaut `pending`), `date_time` → `scheduled_date` nullable + `requested_date`, `duration_minutes` nullable, `notes`/`admin_notes`/`rejection_reason`, présence et paiement sur la leçon, `CHECK (capacity = 1)` ; réservations existantes reprises (une leçon par élève), créneaux vides supprimés, idempotente. Module `lesson` : `Lesson` au format du contrat, `LessonStatus` + pending/rejected, DTO/validators L1–L7 (`status` multiple, `scope`, `date`), repository sur les nouvelles colonnes ; anciennes routes conservées (`POST /` avec `studentId`). Seuils de couverture 88/86/95/88 ; 182 tests.
- [3.4] Migration `008_exam_requests.sql` (D-01, D-26, D-33) : `exams.student_id` → students NOT NULL, `preferred_date`, `message`, `status` ∈ pending/scheduled/completed/cancelled/rejected (défaut `pending`), `location`, `rejection_reason`, `result` ∈ pending/passed/failed, `score`, `notes`, paiement sur l'examen, `date_time` nullable ; inscriptions existantes reprises (un examen par élève), sessions vides supprimées, `exam_registrations` conservée mais plus alimentée ; idempotente. Module `exam` : `Exam` au format du contrat (identité et leçons effectuées de l'élève par jointure), `ExamStatus`, DTO/validators X1–X5 (`result` ∈ passed|failed, score facultatif), repository sur les colonnes 008 ; `RegistrationService`/`RegistrationRepository`, `checkEligibility` et `REQUIRED_LESSONS` supprimés (aussi dans `/api/verification`), routes d'inscription/éligibilité/disponibilité retirées (contrat §9). Seuil branches 88 ; 180 tests.
- [3.5] Migration `009_one_active_enrollment.sql` (D-22) : index unique `students(user_id)`, index unique partiel `enrollment_requests(student_id) WHERE status IN ('pending','approved')`, ancienne `UNIQUE (student_id, school_id)` de 002 levée (une nouvelle demande après refus dans la même école passe) ; la migration s'arrête si des doublons existent. E2 : 409 `CONFLICT` si inscription ou demande active dans n'importe quelle école (`findByUserId`, `findActiveByStudent`), violation d'unicité (23505) en course → 409. 181 tests.
- [4.1] A2 sans `role` : `registerSchema` refuse la clé (400 `VALIDATION_ERROR`), le compte créé est toujours `student` ; prérequis e2e sans `role`. Chemin critique : A2 → E1 passent, blocage à L2 (5.2).
- [4.2] A2 avec `schoolCode` (D-17) : `SchoolCodeRepository.consume` (UPDATE atomique sous conditions actif / non expiré / quota), rôle du code, fiche `instructors` créée dans la même transaction (`InstructorRepository.create` et `UserRepository.create` acceptent le client), 400 `INVALID_SCHOOL_CODE` sinon ; `phone` et `licenseNumber` exigés par Joi avec un code. A2 passe en EXISTE. 190 tests.
- [4.3] `scripts/onboard-school.sh <nom> <adresse> <tél> <email>` : école, grille `pricing` (3 types, montants et durée en option) et code instructeur `INST-<SLUG>-<4 car.>` (quota / expiration en option) en une transaction psql ; idempotent sur l'email (code actif réaffiché) ; seule sortie standard = le code. README « Onboarding d'une école ».
- [4.4] Jetons D-12 / D-23 : `JWT_ACCESS_SECRET` + `JWT_REFRESH_SECRET` (32 car. min, distincts, obligatoires — plus de `JWT_SECRET` ni de repli, compose refuse de démarrer sans), claim `type`, le middleware refuse un refresh token et `/refresh` un access token, durées `1h` / `30d`. Migration `010_admin_seed_password.sql` : le hash de l'admin seedé par 001 ne correspondait à aucun mot de passe → remplacé par celui d'`admin123` (documenté partout), uniquement si le hash cassé est encore en place. 195 tests.
- [4.5] Mobile : `ApiClient` stocke la paire (`@auth_refresh_token`), sur 401 appelle `/api/auth/refresh` une seule fois (401 concurrents partagés, instance axios nue sans intercepteur), stocke la nouvelle paire, rejoue la requête, et efface la session + prévient `AuthContext` (déconnexion) si le refresh échoue ; `AuthContext` stocke le refresh au login/register. `jest-expo` installé (`npm test`, `jest.config.js`, AsyncStorage mocké) : 6 tests de l'intercepteur, axios mocké au niveau de l'adaptateur. `AuthResponse` aligné sur A1/A2 (`accessToken`, `refreshToken`) ; `REFRESH` ajouté à `api.config.ts`. Typecheck mobile : 41 → 37 erreurs restantes (6.1).
- [4.6] Rotation et révocation (D-12) : `sid` (session) sur les deux jetons, `jti` sur le refresh ; `auth:refresh:<jti>` en Redis (TTL 30 j) consommé par GETDEL à chaque `/refresh`, nouvelle paire de la même session ; refresh token déjà consommé → 401 + `auth:session:<sid>:revoked` (toute la famille refusée) ; `/logout` révoque la session du jeton présenté. `ICacheService.take` (GETDEL). A4 et A5 passent en EXISTE. 200 tests.
- [5.0] Profils P1–P11 : `:studentId` = `users.id` (D-28), fiche résolue par `user_id` + `school_id` en une requête ; historiques lus dans `lessons` / `exams` (statuts planifié / passé / annulé, `status` et `price` ajoutés, instructeur par jointure `users`) ; résumé financier sur `lessons` + `exams` (encaissé = payé, dû = planifié ou passé non payé au montant sinon au prix ; Q-17 / Q-18 sans règle particulière, notés) ; P5 `notes` par `user_id` (D-22), P6 `/lessons/:lessonId/mark-paid`, P7 `/exams/:examId/mark-paid`, 404 quand rien n'est touché. `tests/e2e/profiles.e2e.test.ts` (6 tests). Contrat : P1–P11 EXISTE. 201 tests unitaires.
- [5.1] A3 renvoie `schoolId` / `instructorId` pour un instructeur (D-19, `InstructorRepository.findByUserId`) ; S6 `GET /api/schools/:id/students` (élèves autorisés, D-25) ; `src/http/authz.ts` : `SchoolGuard.assertSameSchool(user, schoolId)` → 403 `FORBIDDEN_SCHOOL` (admin exempté), appliqué dans les services à E4, E5, E6, S6, P1–P7 (D-20). `uuidParam` accepte toute forme uuid de Postgres (nil UUID compris). `InstructorRepository` et `SchoolGuard` câblés une fois dans `src/index.ts`. Contrat : A3, S6, E4 EXISTE (27 EXISTE / 4 DIVERGE / 8 MANQUE). 214 tests.
- [5.2] Module `lesson` réécrit sur D-21 / D-32 : L2 `POST /api/lessons` (élève ; école résolue depuis sa fiche `students` autorisée, 403 `NOT_ENROLLED` sinon ; instructeur préféré vérifié de cette école ; leçon `pending` sans instructeur), L1 `GET /api/lessons` scoped (élève : ses leçons ; instructeur : `scope=school` file `pending` de l'école, `scope=mine` ses leçons, les deux par défaut ; admin : tout ; `status` multiple, `date` = jour `Africa/Tunis`), `GET /:id` scoped. Objet `Lesson` du contrat (`studentId` = users.id, `student` et `instructor` joints). Anciennes routes de créneaux, `BookingService` / `BookingRepository` supprimés (`lesson_bookings` plus lue). Chemin critique : A2 → L1 passent (8/10), blocage à L5 (5.3). 210 tests.
- [5.3] L5 `PUT /api/lessons/:id/approve` (instructeur de l'école ; prix de la grille sinon saisi, 400 `PRICE_REQUIRED` — D-30 ; l'approbateur devient l'instructeur — D-32 ; `UPDATE … WHERE status = 'pending'` → 409 en cas de course), L6 `PUT /:id/reject` (instructeur ou admin, motif D-29), L3 `POST /:id/cancel` (élève : sa demande `pending` toujours, sa leçon `scheduled` jusqu'à `LESSON_CANCEL_HOURS` = 24 h avant, 403 `CANCEL_WINDOW_CLOSED` — D-24 ; instructeur / admin : toute leçon de l'école, sans fenêtre). Migration `011_lessons_cancellation.sql` : `cancellation_reason`, `cancelled_by` (auteur, utile pour Q-17). **Q-17 non implémentée** : une leçon payée s'annule comme les autres, paiement inchangé. Étape e2e L5 renommée « approbation de la leçon » ; chemin critique 9/10, blocage à L7 (5.4). 223 tests.
- [5.4] L7 `PUT /api/lessons/:id/attendance` (instructeur de la leçon seulement ; `scheduled` → `completed` ; `student_lesson_stats` incrémenté dans la même transaction, seulement si présent — D-33 ; **Q-18 non implémentée** : une absence est comptée dans le dû comme toute leçon `completed`) ; L4 `POST /api/lessons/book-for-student` (élève inscrit et approuvé dans l'école de l'instructeur, 403 `NOT_ENROLLED` sinon ; prix figé D-30). **Chemin critique v1 (D-15) complet : 10/10.** Contrat : plus aucune ligne DIVERGE (34 EXISTE / 5 MANQUE = X1–X5). 232 tests.
- [5.5] Module `exam` réécrit sur D-01 / D-33 : X2 `POST /api/exams/request` (élève inscrit et approuvé, école résolue, 403 `NOT_ENROLLED`, sans éligibilité — D-26), X1 `GET /api/exams/my-exams` (élève : les siens ; instructeur : tous ceux de son école avec identité et leçons effectuées de l'élève ; admin : tout ; `status` multiple), `GET /:id` scoped ; objet `Exam` du contrat (`studentId` = users.id). Anciennes routes de sessions retirées. `tests/e2e/exams.e2e.test.ts` (4 tests). Contrat : X1, X2 EXISTE (36 EXISTE / 3 MANQUE). 232 tests unitaires.
- [5.6] X3 `PUT /api/exams/:id/schedule` (`pending` → `scheduled`, date + centre), X4 `PUT /:id/reject` (`rejected` + motif, D-33), X5 `PUT /:id/result` (`scheduled` → `completed`, `passed`/`failed`, score facultatif) — instructeur de l'école ou admin (D-20), `UPDATE … WHERE status = …` → 409 en cas de course. `tests/e2e/exams.e2e.test.ts` : 8 tests (X2 → X1 → X3 → X5 → X4 → P8). Contrat : plus aucune ligne MANQUE (39 EXISTE / 2 SUPPRIMÉE). 239 tests unitaires.
- [5.7] `/api/verification/*` supprimé (routes, controller, service, tests ; bloc Nginx retiré — l'application répond 404 comme pour toute route inconnue) ; `GET /api/schools/instructors/:id` verrouillé `admin` (§8). Audit : les routes montées sont exactement les §1–7 du contrat + les routes `admin` de son §8 ; `GET /api/lessons/:id` et `GET /api/exams/:id` conservées scoped. 235 tests unitaires, 27 e2e.
- [5.8] Contrat entièrement EXISTE (39 routes, 0 DIVERGE, 0 MANQUE, §10) ; job e2e bloquant en CI (`continue-on-error` retiré, journaux publiés en cas d'échec). 4 suites e2e, 27 tests.
- [6.1] Hygiène mobile : `ApiClient.get/post/put/delete<T>` typés et tous les `*Service.ts` renvoient `response.data` ; modèles uniques dans `src/models/` au format du contrat (`Lesson`, `Exam`, `Enrollment`, `School`, `Profile`, `User`), enums D-18 (`CODE`/`Manœuvre`/`Parc`, `theory`/`practical`, `passed`/`failed`) avec libellés ; `ApiError.getApiErrorMessage` (D-27 : `message` → `error` → secours) dans tous les `catch` ; `utils/format.ts` (montants — symbole unique, Q-20 —, dates, noms). `BookLessonScreen` : 3 types, date et heure souhaitées (`requestedDate`, D-21), instructeur préféré facultatif et effaçable (D-32), bouton « Request Lesson » sans instructeur sur la fiche école ; `MyLessonsScreen` / `MyExamsScreen` : statuts `pending` / `rejected` (motifs), `paid` / `amount`, retrait d'une demande `pending`. Inscription : plus de `role` (`RegisterScreen`), `InstructorRegistrationScreen` en un seul formulaire → A2 avec `schoolCode` (forcé par la suppression de `SchoolCodeService.ts`, prévu en 6.2). Écrans instructeur et onglets fiche élève alignés sur les objets du contrat (noms réels, `scheduledDate`, `durationMinutes`, P6/P7 sur `/lessons/:lessonId` et `/exams/:examId`) ; les stubs restent pour 6.3 / 6.5. Supprimés : `SchoolCodeService.ts`, chemins du §9, plugin Babel `react-native-dotenv` (+ dépendance). Backend : champ dérivé `name` retiré de S3. `npx tsc --noEmit` : 37 → 0 erreurs ; job CI `mobile` (typecheck + jest-expo). `MINIMAL_DESIGN_GUIDE.md` conservé (référence du design system).
- [6.2] Auth mobile : `AuthContext` appelle A3 après A1 / A2 et stocke `{ id, email, firstName, lastName, role, schoolId?, instructorId? }` (session annulée si A3 échoue), rafraîchit l'identité au démarrage (copie locale conservée hors réseau), `logout` appelle A5 puis efface le stockage local dans tous les cas ; `InstructorDashboard` passe `schoolId` (A3) à `EnrollmentRequests` (alerte si le compte n'est lié à aucune école) ; `EnrollmentRequestsScreen` exige un motif de 10 à 500 caractères (compteur, bouton désactivé, D-29). Tests jest des services (`mockApiClient.ts` : URL, verbe, payload, `response.data`) : `AuthService` (A1, A2 élève / instructeur, A3, A5, erreur propagée), `EnrollmentService` (E1–E6) — 18 tests mobile. L'inscription instructeur en une étape a été livrée en 6.1.
- [6.3] Leçons côté instructeur : `LessonService.getMyLessons(filters?)` (`status` multiple, `scope`, `date`) ; `LessonRequestsScreen` liste la file `pending` de l'école (L1 `scope=school`), affiche l'instructeur préféré (S3, « you » si c'est l'appelant) et le tarif de la grille (S4), planifie par L5 (date et heure pré-remplies avec la date souhaitée, durée de la grille, champ prix seulement sans tarif — D-30, notes pour l'élève) et refuse par L6 (motif 10–500 caractères) ; `TodayLessonsScreen` liste ses leçons du jour (L1 `scope=mine&date=<jour local>`, `toLocalDateKey`) et enregistre la présence par L7 (présent / absent, note 1–5 et commentaire facultatifs ; onglet Completed = leçons déjà pointées). Plus aucun stub sur ces deux écrans. Tests `LessonService` (L1 ×3, L2–L7) — 27 tests mobile. Flux vérifié sur la stack : L5 → L1 (mine, date) → L7.
- [6.4] Réservation directe : `SchoolService.getSchoolStudents` (S6, chemin `SCHOOLS.STUDENTS`, modèle `SchoolStudent`) ; `BookForStudentScreen` charge les élèves autorisés de l'école (`schoolId` de A3), liste filtrable par nom ou email avec sélection (plus de saisie d'email), 3 types D-18, date et heure, durée pré-remplie depuis la grille S4, champ prix seulement sans tarif (D-30), payload L4 `{ studentId (users.id), type, scheduledDate, durationMinutes, price?, notes? }`. Tests `SchoolService` (S1–S4, S6) — 32 tests mobile. L4 vérifié sur la stack avec un élève de S6 (`Manœuvre`, prix de la grille appliqué).
- [6.5] Examens : `ExamService.getMyExams(filters?)` (`status` multiple) ; `ExamRequestsScreen` liste les demandes `pending` de l'école (X1), affiche le message de l'élève et ses leçons effectuées (D-26), planifie par X3 (date et heure pré-remplies avec la date souhaitée, centre obligatoire) et refuse par X4 (motif 10–500 caractères) ; `TodayExamsScreen` liste les examens `scheduled` / `completed` du jour local (X1 n'a pas de filtre de date) et enregistre le résultat par X5 (`passed` / `failed`, score facultatif entier 0–100, notes). Plus aucun stub dans le mobile. Libellés « Schedule » / « Reject » conservés tels quels en attendant Q-19. Tests `ExamService` (X1 ×2, X2–X5) — 38 tests mobile. Flux vérifié sur la stack : X1 → X3 → X1 → X5 sans score.
- [5.8 fix] CI rouge sur `main` depuis 5.6 : seuil de couverture des branches (88 %) non atteint depuis le retrait de `verification` en 5.7 (87,67 %) → deux tests de validation `POST /login` et `POST /refresh` (237 tests, 88,23 %) ; `e2e.yml` : les secrets JWT passent au niveau du job (le compose les exige via `:?`, `docker compose down -v` échouait à chaque exécution) ; `build-images` : `permissions: packages: write` pour le push vers ghcr.io (échouait à chaque exécution, cause présumée, non vérifiable sans les journaux).
