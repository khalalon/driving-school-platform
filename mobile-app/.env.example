# URL du backend vue depuis le téléphone (Nginx sur :80, IP de la machine sur le même Wi-Fi).
# Lecture effective par l'app : voir tâche 0.4 du plan (src/config/api.config.ts).
API_BASE_URL=http://YOUR_IP_ADDRESS:80
