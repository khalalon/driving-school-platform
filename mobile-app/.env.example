# Surcharge locale de expo.extra.API_BASE_URL (app.json) : URL du backend vue depuis le
# téléphone, soit l'IP de la machine sur le même Wi-Fi, Nginx sur le port 80.
# Copier ce fichier en .env (ignoré par git) puis relancer `npx expo start -c`.
EXPO_PUBLIC_API_BASE_URL=http://YOUR_IP_ADDRESS:80
